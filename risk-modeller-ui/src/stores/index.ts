/**
 * Global client-state stores (Zustand) live here. Server state belongs in
 * React Query, not in these stores (see .ai/docs/State-Management.md).
 *
 * No global UI store is needed yet; feature-scoped stores live under
 * `features/<name>/stores/`. This barrel exists as the documented home for
 * cross-feature stores when they are introduced.
 */
export {};
